using PaperGenerateSystem.UI;
using System;
using System.Threading;

namespace PaperGenerateSystem
{
    class Program
    {
        static void Main(string[] args)
        {
            UserInterface UI = new UserInterface();
            UI.Start();
        }
    }
}
